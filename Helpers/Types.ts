export interface ScreenBreakPoints {
  isPhone: boolean;
  isIpad: boolean;
  isNextHub: boolean;
  isLaptop: boolean;
  isDesktop: boolean;
}

export interface ImageProps {
  imageUrl: string;
}

export interface ScrollPosition {
  scrollX: number;
  scrollY: number;
}
